<div class="row" style="margin-top: 0;">
  <div class="col-md-3">
    <h2 class="title">
      <%= page.name %>
    </h2>
  </div>
  <div class="col-md-2 offset-md-7">
    <%= link_to "Все отзывы", reviews_path,class: 'show-more button reviews' %>
  </div>
</div>
  <div class="row">
    <% Review.all.last(2).each do |item| %>
    <div class="col-md-6">
      <%= link_to review_path(item.id),class: 'show-review' do %>
      <div class="review-box">
        <h4><%= item.name %></h4>
        <span><%= item.site %></span>
        <p><%= truncate(item.body,length: 400,separator: ' ') %></p>
      </div>
      <% end %>
    </div>
    <%end %>
  </div>
